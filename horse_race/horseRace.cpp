#include <iostream>

void advance(int horseNum, int* horses);
void printlIne(int horseNum, int* horses);
void isWinner(int horseNum, int* horses);

const MAX_LENGTH = 15;
const MAX_HORSES = 5;

std::random_device rd;
std::uniform_int_distribution<int> dist(0, 1);

int main (){

  int horseName[] {0, 1, 2, 3, 4};
  boolean keepGoing = true;
  
  while (keepGoing){
  
    std::cout << "Press enter to begin the reace" << endl:
    

  }
  
}
